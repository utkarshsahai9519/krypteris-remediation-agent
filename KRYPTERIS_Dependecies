#!/bin/bash

# ==============================================================================
# KRYPTERIS XDR AUTOMATED ENVIRONMENT STABILIZATION SCRIPT
# ==============================================================================
print("[INFO] Initializing environment consolidation layers...")

# 1. Upgrade foundational installer tooling
pip install --upgrade pip

# 2. Install primary layout, chart rendering, and data frameworks
pip install streamlit pandas openpyxl requests plotly

# 3. Install core web API router gates using compatible pairing versions
pip install fastapi==0.115.6 pydantic==2.10.3 starlette==0.45.1 uvicorn

# 4. Force-reinstall stable NumPy to resolve the Numba backend multi-processing worker loop error
pip install --force-reinstall "numpy==2.1.3"

pip install plotly

pip install openpyxl

# 5. Clear the active shell path tracking limits to activate updates
hash -r

print("[SUCCESS] Ecosystem configuration locked down and nominal.")



Clear Hidden GPU Zombie Loops (Before starting anything)

pkill -9 -f backend_api.py || pkill -9 -f vllm || pkill -9 -f streamlit || pkill -9 -f python

cd /workspace/shared/Krypteris
python3 backend_api.py

cd /workspace/shared/Krypteris
python3 -m streamlit run app.py --server.port 8501 --server.headless true --server.enableCORS false --server.enableXsrfProtection false

